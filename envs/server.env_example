# We'll need to recall the Postgres DB env vars (this should match what is in postgres.env_example)
POSTGRES_USER=your_postgres_username
POSTGRES_PASSWORD=your_postgres_pwd
POSTGRES_DB="prefect"
#   The Prefect server can be configured so we can connect to it from _outside_ the container (such as on our local host)
      # If you want to access Prefect Server UI from anywhere other than the Docker host machine, you will need to change
      # PREFECT_UI_URL and PREFECT_API_URL to match the external hostname/IP of the host machine. For example:
      #- PREFECT_UI_URL=http://external-ip:4200/api
      #- PREFECT_API_URL=http://external-ip:4200/api
PREFECT_UI_URL="http://some-ip:4200/api"
PREFECT_API_URL="http://some-ip:4200/api"
PREFECT_SERVER_API_HOST="0.0.0.0"
#    Prefect server can connect to the postgres container via a built-in Docker network
PREFECT_API_DATABASE_CONNECTION_URL="postgresql+asyncpg://$POSTGRES_USER:$POSTGRES_PASSWORD@postgres/$POSTGRES_DB"